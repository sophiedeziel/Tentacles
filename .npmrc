# Enforce yarn as the package manager
engine-strict=true
prefer-workspace-root=true

# Prevent accidental npm usage
# This will cause npm to fail with a helpful error message
# Users should use yarn instead
