query PrinterQuery($id: ID!) {
  printer(id: $id) {
    name
    jobs {
      edges {
        node {
          id
          executable {
            filename
          }
          status
          createdAt
        }
      }
    }
  }
}
